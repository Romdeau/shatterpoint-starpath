{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Shatterpoint Ability",
  "type": "object",
  "properties": {
    "id": { "type": "string" },
    "name": { "type": "string" },
    "type": { "enum": ["Active", "Reactive", "Innate", "Identity", "Tactic"] },
    "cost": { "type": "integer" },
    "description": { "type": "string" }
  },
  "required": ["id", "name", "type", "description"]
}
